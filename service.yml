kind: Service
apiVersion: v1
metadata:
  name: graphql-api-service
spec:
  selector:
    app: graphql-api
  type: LoadBalancer
  ports:
  - name: http
    protocol: TCP
    port: 80
    targetPort: 3000
